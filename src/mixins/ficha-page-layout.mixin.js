export default {
    computed: {
        pageTitle() {
            return this.fichaProfile.pageTitle;
        },
        pageConfig() {
            return this.fichaProfile.pageConfig;
        }
    }
};
